Attribute VB_Name = "modMain"

Sub Main()
    ' Initialization code for the OLE server would go
    ' here. DemoServer requires no initialization, but a
    ' Sub Main is required so that the application won't have
    ' to show a startup form.
End Sub

