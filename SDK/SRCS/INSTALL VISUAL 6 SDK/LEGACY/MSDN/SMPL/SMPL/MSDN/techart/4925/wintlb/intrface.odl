#ifndef __midl
#ifdef WIN32
[
uuid(54674062-3A82-101B-8181-00AA003743D3),
odl,
helpstring("Malloc interface pointer"),
] // Can't use dual or oleautomation because of non HRESULT returns
interface IMalloc : IUnknown {
   long Alloc([in] long cb);
   void Free([in] long pv);
   long Realloc([in] long pv, [in] long cb);
   long GetSize([in] long pv);
   int DidAlloc([in] long pv);
   void HeapMinimize();
}
#endif
#endif
