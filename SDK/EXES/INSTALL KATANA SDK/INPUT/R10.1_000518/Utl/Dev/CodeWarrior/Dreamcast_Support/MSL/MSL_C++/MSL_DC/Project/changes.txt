990409	AS	cctype
Removed the inline functions for c++, must rely on HiC's implementation.

990421	AS
Updated to MSL_5008.1 sources.

990422	AS
The shinobi libraries define bool as an enum.
To support this every template which returns bool must be cast to bool.

990613	AS
Shinobi headers correctly support type bool.  
Changed MSL C++ to use type bool.

990622	AS
Updated to new MSL directory structure.  
Moved MSL inside Dreamcast Support.



