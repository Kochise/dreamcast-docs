
HierSever AppWizard will create project:
Named: $$root$$.mak

Features:
Your HierSever project will have: 
	A default Tree View 
$$IF(WANTS_TEXTVIEW)
	A Text View 
$$ENDIF

Your default sample text will come from:
$$IF(OPEN_FILE)
	The sample file: $$hiersample$$
$$ENDIF
$$IF(USER_INPUT_DATA)
	The data you entered which is stored as 
	text file: "$$root$$.txt"
$$ENDIF
$$IF(NO_INPUT_DATA)
	You will not have sample data
$$ENDIF
